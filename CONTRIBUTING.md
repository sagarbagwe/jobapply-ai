# Contributing

Use small pull requests, add tests, and never include real resumes, credentials, or applicant data. Any site adapter must document the site's permitted interaction model. Features that bypass CAPTCHA, authentication, rate limits, anti-bot controls, or final user confirmation will not be accepted.
